<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MobileCondoManagement.Models"
             xmlns:converters="clr-namespace:MobileCondoManagement.Converters"
             x:Class="MobileCondoManagement.Views.ForgotPasswordPage"
             Title="Forgot Password"
             x:DataType="viewmodels:ForgotPasswordViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#F3F4F6">

    <Grid RowDefinitions="Auto, *">
        <!-- Botão de Login no canto superior direito -->
        <VerticalStackLayout Grid.Row="0" Margin="20, 40, 20, 0" HorizontalOptions="End" VerticalOptions="Start">
            <Button Text="Back to Login"
                    Command="{Binding GoToLoginCommand}"
                    BackgroundColor="#1E90FF"
                    TextColor="White"
                    CornerRadius="8"
                    Padding="10, 5" />
        </VerticalStackLayout>

        <!-- Conteúdo principal alinhado mais acima -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Start" Padding="20" Spacing="20" Margin="0, 150, 0, 0">
                <Label Text="Forgot Password"
                       FontSize="22"
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       TextColor="#1F2937"
                       Margin="0, 0, 0, 20"/>
                <Label Text="{Binding Message}"
                       FontSize="14"
                       HorizontalTextAlignment="Center"
                       IsVisible="{Binding Message, Converter={StaticResource IsNotNullOrEmptyConverter}}"
                       TextColor="{Binding IsSuccess, Converter={StaticResource BooleanToColorConverter}}" />

                <Frame Style="{StaticResource CardFrameStyle}">
                    <Entry Placeholder="Enter your email to receive a password reset link."
                           Text="{Binding Email}"
                           Keyboard="Email"
                           PlaceholderColor="#9CA3AF"
                           TextColor="#1F2937" />
                </Frame>

                <Button Text="Send Reset Link"
                        Command="{Binding SendResetLinkCommand}"
                        IsEnabled="{Binding IsNotBusy}"
                        IsVisible="{Binding ShowSendButton}"
                        Style="{StaticResource PrimaryButtonStyle}" />

                <Button Text="Sign In After Reset"
                        Command="{Binding GoToLoginCommand}"
                        IsVisible="{Binding ShowSignInButton}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        BackgroundColor="#1F2937" />
            </VerticalStackLayout>
        </ScrollView>
    </Grid>

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="CardFrameStyle" TargetType="Frame">
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="15" />
                <Setter Property="BorderColor" Value="#E5E7EB" />
                <Setter Property="HasShadow" Value="True" />
                <Setter Property="Shadow" Value="1, 2, 5, 0" />
            </Style>

            <Style x:Key="PrimaryButtonStyle" TargetType="Button">
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="15, 0" />
                <Setter Property="BackgroundColor" Value="#F59E0B" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>

            <converters:IsNotNullOrEmptyConverter x:Key="IsNotNullOrEmptyConverter" />
            <converters:BooleanToColorConverter x:Key="BooleanToColorConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
</ContentPage>
