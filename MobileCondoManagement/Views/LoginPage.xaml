<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MobileCondoManagement.Models"
             x:Class="MobileCondoManagement.Views.LoginPage"
             x:DataType="viewmodels:LoginViewModel"
             Title="Login">

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Text="Welcome, log into your account"
               FontSize="24"
               FontAttributes="Bold"
               TextColor="White"
               HorizontalOptions="Center"
               Grid.Row="0" />

        <StackLayout Grid.Row="1" Spacing="15" Margin="0,30,0,0">
            <Entry Placeholder="Enter your email"
                   Text="{Binding Email}"
                   Keyboard="Email"
                   BackgroundColor="Transparent"
                   TextColor="White"
                   PlaceholderColor="#A9A9A9" />

            <Entry Placeholder="Enter your Password"
                   Text="{Binding Password}"
                   IsPassword="True"
                   BackgroundColor="Transparent"
                   TextColor="White"
                   PlaceholderColor="#A9A9A9" />
        </StackLayout>

        <Button Text="Login"
                Command="{Binding LoginCommand}"
                IsEnabled="{Binding IsNotBusy}"
                BackgroundColor="#EE9F00"
                TextColor="#041735"
                CornerRadius="10"
                Grid.Row="2"
                Margin="0,20,0,0"/>

        <ActivityIndicator IsRunning="{Binding IsBusy}"
                           IsVisible="{Binding IsBusy}"
                           Color="#EE9F00"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"
                           Grid.Row="3"/>

        <Label Text="Forgot password?"
               TextColor="#EE9F00"
               HorizontalOptions="Center"
               Grid.Row="4"
               VerticalOptions="Start"
               Margin="0,10,0,0">
            <Label.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ForgotPasswordCommand}" />
            </Label.GestureRecognizers>
        </Label>

        <Label Text="{Binding ErrorMessage}"
               TextColor="Red"
               HorizontalOptions="Center"
               Grid.Row="5"
               IsVisible="{Binding HasError}" />
    </Grid>
</ContentPage>